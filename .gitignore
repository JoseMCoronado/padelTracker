build/
.DS_Store
.cache/
compile_commands.json

# firmware/remote built for a DevKit stand-in remote; the tracked sdkconfig
# stays on esp32c3 for the real XIAO hardware.
build-s3/
firmware/remote/sdkconfig.s3*

# Development radio keys (docs/PAIRING.md); never commit real keys.
firmware/keys/dev_keys.h

# idf.py's backup of the previous sdkconfig, rewritten on every config change.
firmware/*/sdkconfig.old

# Simulator runtime data
court-sim-data/
